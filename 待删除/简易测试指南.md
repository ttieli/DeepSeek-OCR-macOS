# 🚀 DeepSeek-OCR 超简易测试指南

## ✅ 当前状态

部署脚本 `deploy_deepseek_ocr.sh` 正在运行中，它会自动完成：
- ✅ 创建 conda 环境 `deepseek-ocr`
- ✅ 克隆 DeepSeek-OCR 代码
- 🔄 安装 PyTorch（进行中）
- ⏳ 安装其他依赖（等待中）

## 📝 等部署完成后，运行测试

### 方法 1：使用新的简化脚本（推荐）

```bash
cd DeepSeek_OCR_for_MacOS
bash test.sh
```

### 方法 2：手动运行

```bash
# 1. 激活环境
source ~/miniforge3/etc/profile.d/conda.sh
conda activate deepseek-ocr

# 2. 进入项目目录
cd "/Users/tieli/Library/Mobile Documents/com~apple~CloudDocs/Project/DeepSeek_OCR_for_MacOS"

# 3. 运行测试
python run_ocr_test.py
```

## 🎯 预期输出

```
============================================================
DeepSeek-OCR 文档转换测试
============================================================

✓ PyTorch 版本: 2.x.x
✓ MPS (Metal Performance Shaders) 可用

找到 1 张测试图片:
  - Screenshot 2025-10-23...png

正在加载 DeepSeek-OCR 模型...
(首次运行会从 Hugging Face 下载模型，约 10GB，请耐心等待)

加载分词器...
加载模型...
✓ 模型加载成功，使用设备: mps

============================================================
开始 OCR 识别...
============================================================

[1/1] 处理: Screenshot 2025-10-23...png
  正在识别...
  ✓ 识别完成
  ✓ 结果已保存到: ocr_output/...
  ✓ Markdown 文件: ..._result.md

============================================================
OCR 识别完成!
============================================================

输出目录: ocr_output
```

## 📂 查看结果

测试完成后，打开：
```bash
open ocr_output
```

找到生成的 Markdown 文件，查看 OCR 识别结果。

## ⚠️ 常见问题

### 1. 部署脚本卡住不动？
等待它完成，PyTorch 安装可能需要 5-10 分钟。

### 2. 提示 "No module named 'torch'"？
确保使用 `bash test.sh` 或手动激活 conda 环境。

### 3. 模型下载很慢？
首次运行需要下载约 10GB 模型，需要良好的网络。

### 4. 找不到 test.sh？
确保在正确的目录：
```bash
cd "/Users/tieli/Library/Mobile Documents/com~apple~CloudDocs/Project/DeepSeek_OCR_for_MacOS"
ls test.sh  # 应该能看到这个文件
```

## 🎉 开始测试

**等 `deploy_deepseek_ocr.sh` 完成后**，运行：

```bash
bash test.sh
```

或者直接：

```bash
bash "/Users/tieli/Library/Mobile Documents/com~apple~CloudDocs/Project/DeepSeek_OCR_for_MacOS/test.sh"
```

---

💡 **提示**：部署脚本完成时会显示"部署完成"的消息。看到后再运行测试。
