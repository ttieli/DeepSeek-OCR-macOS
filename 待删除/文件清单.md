# 📁 DeepSeek-OCR 文件清单

## ✅ 核心文件（必需）

### 主要脚本（4个）

```
DeepSeek_OCR_for_MacOS/
├── install.sh          ⭐ 安装脚本（第一次运行）
├── ocr.sh              ⭐ 使用脚本（日常使用）
├── fix_cuda.py         兼容性修复（自动调用）
└── convert_mmd.py      格式转换（自动调用）
```

### 文档文件（3个）

```
├── 开始使用.md          ⭐ 快速入门指南
├── 使用说明.txt         详细说明
└── 文件清单.md          本文件
```

### 工作目录（2个）

```
├── test_images/        输入目录（放图片）
└── ocr_output/         输出目录（查看结果）
```

**总计**：9 个文件/目录

---

## 📦 其他文件（可选）

以下文件是开发过程中创建的，可以删除：

### 开发和测试文件

```
├── test.sh                     # 旧测试脚本 → 已整合到 ocr.sh
├── run_ocr_test.py             # 旧测试脚本 → 已整合到 ocr.sh
├── run_ocr_mps.py              # MPS 测试版本 → 已废弃
├── deploy_deepseek_ocr.sh      # 旧部署脚本 → 已整合到 install.sh
├── fix_mps_support.py          # 旧修复脚本 → 已整合到 fix_cuda.py
├── install_dependencies.sh     # 旧安装脚本 → 已整合到 install.sh
├── run_ocr_test.sh             # 旧运行脚本 → 已整合到 ocr.sh
├── save_test_image.py          # 测试工具 → 可删除
├── test_ocr.py                 # 测试工具 → 可删除
└── 一键运行.sh                  # 旧脚本 → 已整合到 ocr.sh
```

### 文档文件

```
├── README.md                   # 项目说明 → 参考用
├── README_OCR_TEST.md          # 测试说明 → 参考用
├── 交互式使用指南.md            # 详细指南 → 参考用
├── 使用指南.md                  # 使用指南 → 参考用
├── 快速参考.md                  # 速查表 → 参考用
├── 快速测试指南.md              # 测试指南 → 参考用
├── 最终测试指南.md              # 测试指南 → 参考用
├── CPU模式说明.md               # 技术说明 → 参考用
├── MacOS修复指南.md             # 修复指南 → 参考用
├── 问题诊断.md                  # 诊断文档 → 参考用
├── 格式转换说明.md              # 转换说明 → 参考用
├── 简易测试指南.md              # 测试指南 → 参考用
└── convert_mmd_to_md.py        # 旧转换脚本 → 已整合
```

### 其他目录

```
├── DeepSeek-OCR/               # 源代码目录 → 保留
├── output/                     # 旧输出目录 → 可删除
└── .DS_Store                   # macOS 系统文件 → 自动生成
```

---

## 🧹 清理建议

### 方案 A：仅保留核心文件

保留以下文件即可正常使用：

```
DeepSeek_OCR_for_MacOS/
├── install.sh          ⭐
├── ocr.sh              ⭐
├── fix_cuda.py
├── convert_mmd.py
├── 开始使用.md
├── 使用说明.txt
├── test_images/
├── ocr_output/
└── DeepSeek-OCR/      # 自动下载
```

**可删除的文件**：

```bash
# 在项目目录执行
rm -rf test.sh run_ocr_test.py run_ocr_mps.py deploy_deepseek_ocr.sh
rm -rf fix_mps_support.py install_dependencies.sh run_ocr_test.sh
rm -rf save_test_image.py test_ocr.py 一键运行.sh
rm -rf output/
rm -rf README.md README_OCR_TEST.md 交互式使用指南.md
rm -rf 使用指南.md 快速参考.md 快速测试指南.md 最终测试指南.md
rm -rf CPU模式说明.md MacOS修复指南.md 问题诊断.md
rm -rf 格式转换说明.md 简易测试指南.md convert_mmd_to_md.py
```

### 方案 B：保留所有文档（推荐）

如果想保留所有文档作为参考：

保留核心文件 + 以下文档：
- `交互式使用指南.md` - 详细的模式说明
- `CPU模式说明.md` - 技术原理
- `格式转换说明.md` - 格式转换详解

只删除旧脚本：

```bash
rm -rf test.sh run_ocr_test.py run_ocr_mps.py deploy_deepseek_ocr.sh
rm -rf fix_mps_support.py install_dependencies.sh run_ocr_test.sh
rm -rf save_test_image.py test_ocr.py 一键运行.sh output/
```

---

## 📊 文件大小参考

| 类型 | 大小 |
|-----|------|
| 核心脚本 | < 1MB |
| 文档文件 | < 1MB |
| DeepSeek-OCR 模型 | ~10GB（首次下载） |
| test_images | 取决于图片 |
| ocr_output | 取决于结果 |

---

## 🎯 最简配置

如果只想要最精简的版本：

```
DeepSeek_OCR_for_MacOS/
├── install.sh          # 安装
├── ocr.sh              # 使用
├── fix_cuda.py         # 修复
├── convert_mmd.py      # 转换
└── 开始使用.md          # 说明
```

**5 个文件即可完整运行！**

---

## 💾 备份建议

在清理前，可以创建备份：

```bash
# 备份整个目录
cp -r "DeepSeek_OCR_for_MacOS" "DeepSeek_OCR_for_MacOS_backup"

# 或只备份文档
mkdir docs_backup
cp *.md docs_backup/
```

---

**根据需要选择合适的清理方案！** 🧹
