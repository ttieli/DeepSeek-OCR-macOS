# 📝 .mmd 格式转换说明

## 🤔 为什么是 .mmd 格式？

`.mmd` 是 DeepSeek-OCR 的输出格式，可能是 **"MultiModal Markdown"** 的缩写。

### 特点

- ✅ 保留 HTML 表格（更灵活）
- ✅ 支持复杂布局
- ✅ 包含位置信息
- ❌ 不是标准 Markdown（部分编辑器不支持）

## 🔄 如何转换

### 方法一：使用转换脚本（推荐）

#### 转换单个文件

```bash
python3 convert_mmd_to_md.py result.mmd
```

**输出**：`result_converted.md`（标准 Markdown）

#### 批量转换整个目录

```bash
python3 convert_mmd_to_md.py ocr_output/
```

**输出**：为每个 `.mmd` 文件生成 `_converted.md` 版本

### 方法二：手动转换

1. 打开 `.mmd` 文件
2. 复制内容
3. 使用在线工具转换 HTML 表格
4. 保存为 `.md` 文件

## 📊 格式对比

### .mmd 格式（原始）

```html
<table>
  <tr>
    <td>申请人名称</td>
    <td>2411590568</td>
  </tr>
  <tr>
    <td>所属机构</td>
    <td>北京上地支行</td>
  </tr>
</table>
```

### .md 格式（转换后）

```markdown
| 项目 | 内容 |
|------|------|
| 申请人名称 | 2411590568 |
| 所属机构 | 北京上地支行 |
```

## 🛠️ 转换脚本功能

`convert_mmd_to_md.py` 会自动：

1. ✅ 转换 HTML 表格为 Markdown 表格
2. ✅ 清理 HTML 标签
3. ✅ 添加文档头部和尾部
4. ✅ 保留原始格式
5. ✅ 支持批量转换

## 📁 输出文件说明

转换后会生成两个文件：

```
ocr_output/
  └── [图片名]/
      ├── result.mmd              # 原始 MMD 格式
      ├── result_converted.md     # 自动转换版本
      └── 完整版.md               # 手工美化版本 ⭐
```

**推荐**：
- **日常使用**：`result_converted.md`（自动转换）
- **重要文档**：`完整版.md`（手工美化，格式更好）

## 🎯 使用示例

### 示例 1：转换单个文件

```bash
cd "/Users/tieli/Library/Mobile Documents/com~apple~CloudDocs/Project/DeepSeek_OCR_for_MacOS"

python3 convert_mmd_to_md.py "ocr_output/8cedbcd6c0d3f0c595276a5a5bd5119d/result.mmd"
```

**输出**：
```
✓ 转换完成
  输入: result.mmd
  输出: result_converted.md
```

### 示例 2：批量转换

```bash
python3 convert_mmd_to_md.py ocr_output/
```

**输出**：
```
找到 5 个 .mmd 文件

[1/5] 转换: result.mmd
✓ 转换完成
  输入: ocr_output/file1/result.mmd
  输出: ocr_output/file1/result_converted.md

[2/5] 转换: result.mmd
...

转换完成: 成功 5，失败 0
```

## 💡 提示

### 查看 .mmd 文件

虽然是 HTML 格式，但可以直接用文本编辑器打开：

```bash
# 在 VS Code 中打开
code result.mmd

# 在终端中查看
cat result.mmd

# 在 Markdown 预览器中打开（部分支持）
open -a "Typora" result.mmd
```

### 选择合适的格式

| 需求 | 推荐格式 | 原因 |
|-----|---------|------|
| 在线分享 | `.md` | 通用兼容 |
| 本地查看 | `.mmd` 或 `.md` | 都可以 |
| 编辑修改 | `.md` | 更易编辑 |
| 保留原始 | `.mmd` | 完整信息 |

### 编辑器支持

| 编辑器 | .mmd 支持 | .md 支持 |
|--------|-----------|---------|
| VS Code | ⚠️ 部分 | ✅ 完全 |
| Typora | ⚠️ 部分 | ✅ 完全 |
| Obsidian | ❌ 不支持 | ✅ 完全 |
| 记事本 | ✅ 可查看 | ✅ 可查看 |

## 🔧 自定义转换

如果需要自定义转换逻辑，编辑 `convert_mmd_to_md.py`：

```python
# 修改表格样式
def convert_html_table_to_markdown(html_content):
    # 在这里添加自定义转换逻辑
    pass
```

## 📚 相关文件

- **`convert_mmd_to_md.py`** - 转换脚本
- **`完整版.md`** - 手工美化的示例
- **`result.mmd`** - 原始 OCR 输出

## ⚡ 快速参考

```bash
# 转换单个文件
python3 convert_mmd_to_md.py file.mmd

# 批量转换
python3 convert_mmd_to_md.py ocr_output/

# 查看帮助
python3 convert_mmd_to_md.py
```

---

**现在你可以轻松转换 .mmd 文件了！** 🎉
