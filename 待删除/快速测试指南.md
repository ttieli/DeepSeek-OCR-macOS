# DeepSeek-OCR 快速测试指南

## 🎯 三步完成测试

### 第一步：安装依赖（首次运行必需）

```bash
cd "/Users/tieli/Library/Mobile Documents/com~apple~CloudDocs/Project/DeepSeek_OCR_for_MacOS"
./install_dependencies.sh
```

**说明**：
- 会自动安装 PyTorch 和其他必需的库
- 首次安装约需 5-10 分钟
- 只需运行一次

---

### 第二步：准备测试图片

**方式 A：使用现有图片**（已准备好）
```bash
# 你的测试图片已经在 test_images/ 目录：
# ✅ Screenshot 2025-10-23 at 18-11-49 git-sync-workflow.html Claude Claude.png
```

**方式 B：添加自己的图片**
```bash
# 只需将任意图片复制到 test_images 目录
cp /path/to/your/image.png test_images/
```

支持格式：PNG、JPG、JPEG、BMP、WEBP

---

### 第三步：运行 OCR 测试

```bash
cd "/Users/tieli/Library/Mobile Documents/com~apple~CloudDocs/Project/DeepSeek_OCR_for_MacOS"
./run_ocr_test.sh
```

**首次运行会发生什么？**
1. 自动下载 DeepSeek-OCR 模型（约 10GB，需要一些时间）
2. 加载模型到内存
3. 对 test_images 中的所有图片进行 OCR 识别
4. 将结果保存为 Markdown 格式

---

## 📁 查看结果

测试完成后，结果保存在 `ocr_output/` 目录：

```
ocr_output/
  └── Screenshot 2025-10-23 at 18-11-49 git-sync-workflow.html Claude Claude/
      ├── Screenshot 2025-10-23 at 18-11-49 git-sync-workflow.html Claude Claude_result.md
      └── [其他输出文件]
```

打开 `_result.md` 文件查看识别的文本内容（Markdown 格式）。

---

## ⚡ 一键测试命令

如果你已经安装过依赖，可以直接运行：

```bash
cd "/Users/tieli/Library/Mobile Documents/com~apple~CloudDocs/Project/DeepSeek_OCR_for_MacOS" && ./run_ocr_test.sh
```

---

## 🔍 故障排除

### 问题 1：提示找不到 torch 模块
**解决**：运行 `./install_dependencies.sh` 安装依赖

### 问题 2：模型下载太慢
**解决**：需要良好的网络连接访问 Hugging Face，首次运行需要下载约 10GB 模型

### 问题 3：内存不足
**解决**：确保 Mac 至少有 16GB 可用内存

### 问题 4：权限被拒绝
**解决**：
```bash
chmod +x install_dependencies.sh
chmod +x run_ocr_test.sh
```

---

## 📊 性能说明

- **首次运行**：下载模型 + 识别，约需 10-30 分钟
- **后续运行**：仅识别，每张图片约 1-5 分钟
- **设备支持**：自动检测 MPS（Apple Silicon）或使用 CPU

---

## 💡 提示

1. **测试多张图片**：将多张图片放入 test_images 目录，脚本会自动处理所有图片
2. **查看日志**：测试过程中终端会显示详细进度
3. **模型位置**：首次下载的模型保存在 `~/.cache/huggingface/` 目录
4. **重复测试**：可以随时添加新图片并重新运行测试脚本

---

## 🎉 开始测试

现在运行：
```bash
cd "/Users/tieli/Library/Mobile Documents/com~apple~CloudDocs/Project/DeepSeek_OCR_for_MacOS"
./install_dependencies.sh
```

安装完成后运行：
```bash
./run_ocr_test.sh
```
